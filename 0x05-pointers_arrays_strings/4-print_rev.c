#include "main.h"

/**
 * print_rev - Prints a string in reverse, with a new line
 *
 * @s: The string to reverse
 *
 * Return: nothing
 */

void print_rev(char *s)
{
	int i = 0;

	while (s[i] != '\0')
		i++;

	return (i);
}
